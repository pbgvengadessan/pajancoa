How it works:

Student types their registration number, no login or password.
The page checks it against all 66 records (embedded directly in the page, no database needed) and shows the matching student's name to confirm.
A download button then serves their exact hall ticket PDF from the tickets/ folder.
Wrong or unlisted numbers get a clear "not found" message.
